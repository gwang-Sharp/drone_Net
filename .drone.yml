kind: pipeline
type: docker
name: default

steps:
- name: buildProject
  image: mcr.microsoft.com/dotnet/aspnet:7.0
  commands:
  - dotnet build
  - dotnet publish
  - dotnet ./drone/src/bin/Debug/net7.0/droneService.dll
