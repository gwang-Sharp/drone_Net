kind: pipeline
name: default

steps:
- name: test
  image: mcr.microsoft.com/dotnet/sdk:7.0
  commands:
  - docker build -t droneService .
  - docker run -p 8080:8080 -d -name droneService1 droneService
